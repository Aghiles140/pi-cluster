apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tailscale
resources:
  - ../../base/tailscale-operator/
  # Created (already SOPS-encrypted) by scripts/tailscale-oauth-secret.sh.
  # Run it BEFORE committing, or the infrastructure-controllers
  # Kustomization fails (and Renovate stops reconciling with it).
  - operator-oauth-secret.yaml
